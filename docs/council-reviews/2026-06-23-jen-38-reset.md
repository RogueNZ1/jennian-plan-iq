# JEN-38 Council Reset - 23 Jun 2026

## Status

Council verdict: not ready. Do not continue the current JEN-38 build as-is.

The current work improves diagnosis but does not recover Fenner pricing. The ledger remains at
`0/17` production-priceable rows, `0.00m2` priceable area, `0/17` confirmed floor widths, and
`0/17` face-aware elevation dimensions against the signed `58.13m2` Fenner opening witness.

## Decision

Freeze perimeter/exterior-wall trace work inside JEN-38. Keep it as a separate diagnostic track
until it produces a trusted closed loop on the four-job rail.

Use the Fenner opening ledger as the only recovery scoreboard. Soft elevation hints and
non-face-aware dimension matches are useful search evidence, but they are not recovered heights and
must not price a row.

## Acceptance

Fenner expected-fail turns green only when deterministic product code recovers the signed opening
witness row by row. Each priced row must independently prove:

- physical floor-plan opening identity
- local printed width or width witness
- same-face elevation height/type
- row-level `W x H x face` agreement

No aggregate-only pass counts. No blind standard heights. No borrowing dimensions from interior,
slab, alfresco, concrete, room-width, or drafting artefact evidence.

## First Pilot Rows

The first recovery proof should target three distinctive rows only:

- Garage Door 1: `4.8x2.1`
- Lounge slider: `3.6x2.1`
- Family slider: `3.0x2.1`

One correct end-to-end priced row is more valuable than many soft hints. Scale only after the pilot
chain is proven.

## Tolerance Correction

The active aggregate golden-job materiality rail is `0.5m2`
(`GOLDEN_AGGREGATE_OPENING_AREA_TOLERANCE_M2 = 0.5`). Stale `2m2` doctrine has been corrected.
This is an aggregate rail only and must not hide candidate-level errors.
