
## 12 Jun 2026 — demo day + pipeline-safety batch (promoted same night)
- HEAD OFFICE DEMO: live run start-to-finish (JM-0031, 1:32pm) with full stack — geometry HIGH-confidence floor/walls, vector-won garage door WITH visible reconciliation flag. Rehearsal JM-0030 at 1:27. JM-0020 untouched throughout (validated 11/11 then 12/12).
- GEOMETRY OUTAGE ROOT CAUSE: the LIVE Railway service (the one wearing jennian-iq-geometry-api-production.up.railway.app — sits in the "Jennian IQ" Railway project; duplicate abandoned twins elsewhere caused 2h of misdirected fixes) lost its env vars; GEOMETRY_API_KEY absent → every /measure 401 since ~10 Jun, silently swallowed by catch→null. Fixed live at ~1:25pm. KEY ROTATION PENDING (current key exposed in two chats). Railway twins cleanup PENDING.
- SHIPPED TONIGHT (this promotion): geometry-less runs flag LOUD (compose seam + ⚑⚑ export gate); re-run dialog corrected (old copy falsely promised no overwrite — enabled the 10:52 demo-job corruption, restored same morning via DB surgery); vite:preloadError one-shot reload (post-deploy dead buttons); internal_wall_lm suppressed in export until P2 ribbon-trace (live audit: 7lm vs ~50+ real); all export dates NZT (B5 was UTC = stamped yesterday after 1pm); garage reconciliation flag names sectional-callout resolution.
- EXPORT FIX (morning, already live): no-silent-drop — every window placed or ⚑ UNPLACED; zero-window DO-NOT-PRICE gate.
- OPEN: PAT regenerate w/ Workflows + engine-repo scope → authenticated deploy gate + geometry-probe matrix; Beddis/Young golden re-runs + re-freeze; P1–P6 fortnight (verification page ships with EVERY takeoff — acceptance criterion); door-engine wiring as live source; bed3/wardrobe-1620/laundry answers from HC.
