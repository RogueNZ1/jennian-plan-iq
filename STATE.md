## 21 Jun 2026 - JEN-38 garage blocker simplification and West Street run
- JEN-38 STRICT CLEANUP: removed the accidental garage-door over-block caused by separator/string handling. Added `garage-door-size.ts` as the single tiny numeric reader/normaliser for garage-size labels, so composed/reconciled garage truth is read from numbers (`4.8x2.1`) before display formatting. Visual garage disagreement against a recovered composed garage size is now a warning, not a global opening-pricing block; missing/unusable composed garage truth still remains an error.
- GARAGE LOCAL TRUTH RESTORED: compose now normalises garage labels to plain `WxH` before reconciliation. A recovered vector/elevation sectional door can now price locally (`4.8x2.1 = 10.08m2`) even when a weaker Visual QS garage witness disagrees. Aggregate opening/cladding totals still block when the actual window/opening set is unresolved; this does not relax Fenner's signed `58.13m2` target.
- WEST STREET RUN THROUGH LOCAL BENCHES: ran the committed West Street fixture through the local truth rails. Plan-text confirmed garage footprint `4.0x5.95 = 23.8m2` and preserved title-block cladding `46.7` as title data; routed windows stayed 8/8 against Haydon's hand-verified workbook (`BED2`, `WC`, `BATH`, `MASTERBEDROOM`, `DINING`, `LOUNGE`, `ENSUITE`, `BED3`). Door bench stayed green at singles `11`, doubles `2`, cavity `2`, barn `0`, flags `0`. Wall-trace stayed green at `68.7lm / 32 ribbons` with the known joinery bias still documented.
- VERIFIED: focused visual garage/opening tests green (`22 passed`); West Street plan-text/door/wall benches green (`11 passed`); compose/export safety group green (`41 passed`); touched-file ESLint green; `npx tsc --noEmit` green; full `npx vitest run` green (`754 passed / 1 expected fail / 26 skipped`); `npx vite build` green; `npm run postbuild` green with geometry-proxy guard.
- NOT SOLVED BY THIS SLICE: Fenner still needs candidate-level external opening recovery before it can price to `58.13m2`. This slice removes a false garage brake; it does not pretend the remaining window/elevation reconciliation gap is solved.

## 21 Jun 2026 - JEN-38 live Fenner run after 50mm rule
- LIVE PRODUCTION RUN: after promoting `8adb781` to `main` and verifying both `www.jennianiq.nz` and `jennian-iq-prod.pages.dev` served build `8adb781aae161270d6fa456a0526e96a117a6733`, ran Fenner through production with the controlled floor-plan/elevation PDFs. Saved live job `JM-CODEX-1782011310717` (`ced8ec8e-51b2-4da8-b191-506477d31bb8`), takeoff run `0dbcf7e1-a12a-4b9a-8a4a-4edd47253f72`, completed with no run error.
- LIVE RESULT: floor area `249.9m2`, external wall `89.1lm`, window count `15`, but `total_opening_sqm`, `glazed_sqm`, and `external_wall_area_m2` remained null because opening pricing stayed blocked. `garage_door_size` was recovered as `4.8x2.1`, while the opening-pricing block flag still said the composed takeoff had no usable garage door size. That contradiction is now a concrete follow-up bug.
- GUARD WORKED: the new same-place `50mm` rule prevented the previously dangerous `PANTRY` gap promotion. The floor-plan gap had a `33mm` width delta against elevation evidence, but the elevation face was still unknown/generated, so it stayed review-only instead of entering priced openings.
- PRODUCT STILL NOT ACCURATE: Fenner remains blocked against the signed manual opening truth (`58.13m2` total openings, `48.05m2` garage-door-excluded openings). The live run still shows noisy evidence: elevations overcounted (`38` window candidates / `41` glazed candidates, `43` elevation opening candidates, `21` undimensioned), some vision slider/window candidates have implausibly small dimensions, and many measured floor-plan gaps still lack a trustworthy face/place/height witness.
- NEXT JEN-38 ORDER: first fix the garage-door block contradiction; then demote impossible vision slider/window evidence before it can look locally priced; then improve external-gap classification and position-aware floor-plan/elevation matching so good local evidence can price while bad witnesses remain quarantined. Do not relax the `50mm` confirmation rule to make Fenner pass.

## 21 Jun 2026 - JEN-38 50mm same-place confirmation rule
- JEN-38 STRICT SLICE: Haydon's human-QS rule is now explicit in the floor-plan/elevation bridge. A measured floor-plan gap and elevation opening may remain supporting evidence under the broader matcher, but only a same-face/same-place elevation witness within `50mm` of the floor-plan ruler width is marked `measurementCheck: confirmed` and allowed to promote into priced QS openings.
- FAIL-CLOSED NEAR-MATCHES: same-face elevation candidates with wider deltas (for example `70mm`) stay review-only even if they look plausible. This keeps "close enough to inspect" separate from "confirmed enough to price" and prevents the engine from turning approximate elevation evidence into money.
- REVIEW EVIDENCE CLARIFIED: opening evidence now distinguishes confirmed width/height support from same-face-but-outside-50mm support and from generated/unknown-face support.
- NOT SOLVED BY THIS SLICE: Fenner/JM-0056 still remains the honest expected-fail (`32.98m2` deterministic priced evidence vs `58.13m2` manual truth). This slice tightens the confirmation contract; the next recovery work still has to find and classify more real external floor-plan gaps.
- VERIFIED: focused elevation/promotion/compose/Fenner tests green (`28 passed / 1 expected fail`); touched-file ESLint green; `npx tsc --noEmit` green; full `npx vitest run` green (`753 passed / 1 expected fail / 26 skipped`); `npx vite build` green; `npm run postbuild` green with Cloudflare Pages worker output and geometry-proxy guard.

## 21 Jun 2026 - JEN-38 scale-ruler gap evidence guard
- JEN-38 STRICT SLICE: weak or generated elevation-face support is now evidence-only. `promoteFloorPlanGapOpenings` only promotes a measured floor-plan gap into priced openings when the elevation match has a real face match, so Fenner-style `elevation-face-*` candidates can no longer lend a plausible height/type to the wrong wall gap.
- SCALE-RULER PRIMITIVE ADDED: `scale-ruler.ts` makes the 1:N plan-scale conversion explicit for floor-plan gap measurement. `detectFloorPlanGaps` now measures PDF point gaps through a named ruler (`mmToPdfPoints`, `pdfPointsToMm`, `measureGapWidthMm`) instead of burying the conversion in local helpers. This locks the human-QS premise into code: measure the drawn gap first, then use text/elevation/schedule as witnesses.
- REVIEW EVIDENCE CLARIFIED: review-only opening evidence now says when elevation width/height evidence exists but the elevation face is not matched to the floor-plan wall, and keeps the candidate unpriced until height/type/face are confirmed.
- NOT SOLVED BY THIS SLICE: Fenner/JM-0056 remains the honest expected-fail (`32.98m2` deterministic priced evidence vs `58.13m2` manual truth). This slice prevents a wrong recovery path and strengthens the measurement primitive; the next JEN-38 work is external-wall/standalone-width recovery so good gaps can be priced while bad witnesses stay quarantined.
- VERIFIED: focused opening tests green (`31 passed / 1 expected fail`); touched-file ESLint green; `npx tsc --noEmit` green; full `npx vitest run` green (`750 passed / 1 expected fail / 26 skipped`); `npx vite build` green; `npm run postbuild` green with Cloudflare Pages worker output and geometry-proxy guard.

## 21 Jun 2026 - access and golden-jobs evidence checklist
- OPERATING CHECKLIST ADDED: created `docs/access-and-golden-jobs.md` to turn the missing-access list into an auditable plan. It records current access state, missing operator access, read-only Supabase need, canonical QS master decision, golden-job pack structure, SharePoint/source-folder expectations, and Haydon Council output handling. This is deliberately an evidence/access slice, not a product-code slice.
- JENNIAN IQ OPERATOR ACCESS CONFIRMED: production app access is available through the existing internal operator account `test@jennian-iq.internal`. Codex authenticated to `https://www.jennianiq.nz/jobs` and verified the Jobs table, Upload New Plan action, user menu, and recent JM-0052 through JM-0056 runs were visible. Do not treat IQ app access as missing unless this account is disabled/deleted or login fails; never record the password in Git, STATE.md, Linear, or SharePoint notes.
- LIVE ACCESS RE-VERIFIED: same authenticated production session was checked again on `https://www.jennianiq.nz/jobs`; the page showed Jobs, Upload Plan, user menu, and rows for JM-0052 through JM-0056. This keeps the access state fresh rather than memory-derived.
- LOCAL SOURCE CHECK: observed partial repo fixtures under `tests/fixtures` for `15a`, `beddis`, `christian`, `fenner`, `harrison`, `oneil`, and `young`. Observed local QS master candidates in `QUANTITY SURVEYING/JENNIAN MASTER SPREADSHEET`; Haydon confirmed `Jennian IQ MASTER.xlsm` is the canonical master on 21 Jun 2026.
- SHAREPOINT GOLDEN FOLDER CONFIRMED: Haydon shared the SharePoint folder link for `Shared Documents/QUANTITY SURVEYING/Jennian IQ`. Direct unauthenticated/tool web access returns Microsoft forms-auth `403`, but the same folder is locally synced and readable at `C:\Users\Haydon\Jennian Homes Manawatu\Company - Documents\QUANTITY SURVEYING\Jennian IQ`. Top-level confirmed folders/files include Christian, Beddis, 29A West St, Fenner, and `Jennian IQ MASTER.xlsm`.
- SECONDARY NOTES BACKUP ADDED: with Haydon's approval, created `_Project Notes` inside the synced SharePoint folder with `README.md` and subfolders for `council-reviews`, `golden-job-index`, `trial-readiness`, and `decisions`. This is a secondary evidence/notes backup only; repo `STATE.md` and Linear remain the primary shipped-work audit trail, and secrets must not be stored there.
- GOLDEN JOB INDEX ADDED: created `docs/golden-job-index.md` and mirrored a sanitized backup note to SharePoint `_Project Notes/golden-job-index/2026-06-21-golden-job-index.md`. The index records controlled source paths, hashes, truth status, current tests, and next required slices for Fenner and Christian. Fenner now has an explicit signed opening truth source from `Fenner Specifications.xlsm`; Christian is deliberately marked as a regression rail, not signed pricing truth.
- QS MASTER CONFIRMED: Haydon confirmed `Jennian IQ MASTER.xlsm` is the canonical QS master on 21 Jun 2026. The SharePoint `Jennian IQ` copy and `JENNIAN MASTER SPREADSHEET` copy are byte-identical with SHA-256 `84D45808AFD75D3BC816DC768350BF087C1A2F77EFC07255CD07976720135D63`. Read-only workbook structure check found `IQ Import` and `5. Data Input House `. IQ exports must obey this workbook until Haydon explicitly supersedes it.
- FENNER TRUTH CELLS VERIFIED READ-ONLY: in `Fenner Specifications.xlsm`, sheet `5. Data Input House `, verified `G73=58.13`, `G67=10.08`, `G68=0`, `G75=48.05`, and `D21=155.71`; formula view confirms `G75=G73-G67-G68` and `D21=(D19*D20)-G73`.
- LINEAR UPDATED: created `JEN-39 Provision operator access and golden-job evidence pack`, attached a Linear document mirroring the checklist, and linked/commented dependencies from `JEN-27`, `JEN-29`, and `JEN-38`. This keeps the Head Office trial gate and Fenner recovery tied to controlled evidence rather than chat uploads.
- LINEAR GOLDEN INDEX UPDATED: added a JEN-39 comment and attached Linear document `Golden job index - 21 Jun 2026` (`https://linear.app/jennianiq/document/golden-job-index-21-jun-2026-679a9abe8628`) with the verified app access, canonical master hash, Fenner truth cells, Christian truth status, human QS doctrine, and remaining blockers.
- NOT SOLVED BY THIS SLICE: no Supabase read-only role has been provisioned, Christian is not signed money truth, and Fenner remains an engine expected-fail despite having signed opening truth. Immediate next decisions/work: provision read-only Supabase access or approved alternative, sign Christian QS truth, and continue JEN-38 candidate-level Fenner reconciliation.

## 21 Jun 2026 - JEN-38 candidate-level opening truth slice
- JEN-38 STRICT SLICE: moved opening reconciliation from the old whole-job demotion model toward candidate-level truth without pretending Fenner is solved. When Visual QS reconciliation blocks aggregate opening pricing, `composeTakeoff` now preserves locally valid priced opening candidates and their evidence ledger, but leaves aggregate money fields unpriced (`window_count`, `total_opening_sqm`, `glazed_sqm`, and derived external wall area stay null). One bad witness no longer erases good local evidence, and good local evidence no longer authorises an incomplete cladding deduction.
- EXPORT FAIL-CLOSED ON PARTIAL TRUTH: QS export now treats blocked partial `openings[]` as review evidence only. `windowsByRoom` fallback is suppressed while opening pricing is blocked; the flat QS input sheet prints candidate rows as `REVIEW ONLY - opening pricing blocked` and withholds the opening totals block; the drop-in paste sheet keeps window slot rows zero, blanks the pasted window-count cell, and prints `Less openings: NOT COMPUTED` / `NET CLADDING: NOT COMPUTED`. Opening schedule projection also writes blocked aggregate candidates as review-only blocked rows even if some candidates were locally priced, so the Windows & Doors table can inspect evidence without making it priceable.
- FACE-AWARE ELEVATION MATCHING: `matchElevationToFloorPlanGaps` now uses the floor-plan room side to derive the expected opposite elevation face when both sides are cardinal. Known wrong-face elevation openings cannot support a floor-plan gap; generated or unknown face labels fail open; repeated same-face/same-width candidates still remain ambiguous. This closes the width-only soundness hole before candidate-level promotion becomes more permissive.
- NOT SOLVED BY THIS SLICE: Fenner/JM-0056 remains the honest expected-fail. Manual truth remains `58.13m2` total openings and `48.05m2` garage-door-excluded openings. This slice makes the engine more truthful and less self-sabotaging; it does not yet recover the full QS-accurate Fenner opening set. Next JEN-38 work is the actual candidate-level reconciliation/recovery that prices supported candidates and quarantines only unsupported ones.
- VERIFIED: focused candidate/export/elevation tests green (`39 passed`); touched-file ESLint green; `npx tsc --noEmit` green; full `npx vitest run` green (`746 passed / 1 expected fail / 26 skipped`); `npx vite build` green; `npm run postbuild` green with Cloudflare Pages worker output and geometry-proxy guard.
- DEPLOYED THROUGH NORMAL PRODUCTION PATH: code commit `751b490 fix(takeoff): preserve local opening evidence under block` was pushed to `convergence`, fast-forwarded to `main`, and live-verified on `jennian-iq-prod.pages.dev`, `jennianiq.nz`, and `www.jennianiq.nz` serving `/version.json` build `751b490331d30f871004721401b3b8a507f914ff`. Geometry proxy health on all three domains returned `{"status":"ok","version":"1.3.0"}`.

## 21 Jun 2026 - blocked-opening guard note-source cleanup
- CLAUDE REVIEW FOLLOW-UP: confirmed the `32d58cf` human-path guard was the right accident-prevention slice, and tightened one robustness edge: `isBlockedReviewOnlyOpening` now derives note matching from `BLOCKED_OPENING_REVIEW_ONLY_NOTE` instead of duplicating hard-coded note fragments. This keeps the source-evidence marker and note marker aligned if the operator wording changes later. Added focused coverage for the full canonical note and its heading form.
- VERIFIED: focused guard/projection tests green (`12 passed`); `npx tsc --noEmit` green. This remains a client/app accident-prevention guard, not a database/RLS security boundary; a future structured `pricing_status` field would be the durable migration path if direct API tamper-resistance becomes a requirement.

## 20 Jun 2026 - review-only opening approval/export gate
- COUNCIL RED TEAM BLOCKER CLOSED LOCALLY: Haydon Council found that the `d44459a`/`e3dc39f` truthfulness slices correctly blocked opening pricing, but still let review-only blocked opening candidates appear in the Windows & Doors review table as if they could be confirmed, pushed to pricing modules, or exported as a normal Opening Schedule CSV. That was a product-trust blocker because a human could accidentally promote non-priceable evidence after the engine had done the right fail-closed thing.
- SHARED BLOCKED-OPENING GUARD: added a canonical `opening-review-guards` helper and wired the blocked marker into `opening-schedule-projection`, the Windows & Doors review UI, `updateOpening`, `pushMeasurementToModule`, QS Opening Schedule CSV export, module recalculation, and the quick export print surface. Review-only blocked rows now show a visible warning/badge/evidence note, cannot be confirmed, cannot be pushed to modules, and block Opening Schedule CSV export until reconciled. If an older linked module item is recalculated from a blocked opening source, it is marked review-required instead of refreshed from blocked evidence.
- VERIFIED: focused guard/projection tests green (`11 passed`); `npx tsc --noEmit` green; full `npx vitest run` green (`740 passed / 1 expected fail / 26 skipped`); `npx vite build` green; `npm run postbuild` green with Cloudflare Pages worker output and geometry-proxy guard; touched-file ESLint green. Full local `npm run lint` still fails on the known Windows CRLF Prettier noise across untouched files, so it remains unusable as a Windows-local whole-repo signal until the line-ending cleanup is finished.
- DEPLOYED THROUGH NORMAL PRODUCTION PATH: code commit `32d58cf fix(review): block review-only openings from pricing paths` was pushed to `convergence`, fast-forwarded to `main`, and deployed by GitHub Actions run `27855195528` (`deploy-pages`) with companion `tests` run `27855195532` and `secret-scan` run `27855195534`, all green. Live verification showed `jennian-iq-prod.pages.dev`, `jennianiq.nz`, and `www.jennianiq.nz` serving `/version.json` build `32d58cfee7eda69ac864cfca3eca7d80aa4671a0`; geometry proxy health on both custom domains returned `{"status":"ok","version":"1.3.0"}`.
- NOT SOLVED BY THIS SLICE: Fenner/JM-0056 remains an expected-fail on pricing accuracy; manual truth is still `58.13m2` total openings, `48.05m2` garage-door-excluded openings, and garage door `4.8x2.1`. This slice prevents blocked evidence from becoming priceable by accident; it does not recover the missing QS-accurate opening set.

## 19 Jun 2026 - JM-0056 blocked-run truth and garage evidence hardening
- JM-0056 COUNCIL AUDIT RESULT: latest Fenner run proved the d44459a fail-closed money gate works, but the artefacts were not QS-trustworthy. Workbook pricing blocked correctly, yet the verification printout still displayed raw Visual QS counts and an estimated `5.4x2.4` garage size in a way that looked usable. Manual Fenner truth remains `58.13m2` total openings, `48.05m2` garage-door-excluded openings, and garage door `4.8x2.1`; this run did not recover full price truth.
- VERIFICATION/PDF HONESTY FIX: `buildVerificationModel` now carries explicit `windows.pricingBlocked`, `pricingBlockFlags`, and review-only Visual QS totals. When opening pricing is blocked, the PDF Windows section prints `OPENING PRICING BLOCKED`, suppresses priced Visual QS totals/garage counts, suppresses per-room fallback rows, and marks Visual QS candidates as review-only. If no exported garage row exists, garage size displays `Blocked - verify manually` instead of a raw visual/elevation disagreement.
- REVIEW NOTES / REVIEW TABLE FIX: global opening-pricing blocks now demote any stale `priced` evidence labels to `not priced; review` in Review Notes. Wizard upload projection can now write known blocked opening evidence candidates into `opening_schedule` as review-only rows (with `REVIEW ONLY - opening pricing blocked` notes) so the Windows & Doors review surface is not empty when evidence exists. Unknown raw gaps stay in Review Notes rather than flooding the review table.
- GARAGE EVIDENCE HARDENING: `composeTakeoff` now prefers elevation vector garage-door candidates over a disagreeing visual garage read, snaps the widest plausible elevation garage candidate to the shared QS garage size classifier, and carries the recovered `4.8x2.1` candidate as vector evidence while the unresolved visual conflict remains review-only. This fixes the Fenner-style `5.4x2.4` visual poison at the evidence layer without pretending the full opening set is price-ready.
- VERIFIED: focused tests for verification model/export flags/opening projection/visual compose all green; `npx tsc --noEmit` green; touched-file ESLint green; touched-file Prettier green; full `npm run test` green (`734 passed / 1 expected fail / 26 skipped`); `npm run build` green. The remaining Fenner recovery test is still an expected-fail because deterministic evidence currently supports only part of the `58.13m2` manual truth; the next slice must be candidate-level opening reconciliation, not a whole-job count downgrade.

## 19 Jun 2026 - JM-0055 opening pricing quarantine and review bridge
- JM-0055 FENNER FAILURE ROOT CAUSE: latest wizard run `JM-0055` priced an impossible visual witness (`Entrance`, source `vision`, `1.6m x 90m = 144m2`) into canonical `openings[]`, inflating `total_opening_sqm` / cladding less-openings to `187.89m2` against Fenner manual truth `58.13m2`. This proved the missing layer was not spreadsheet export formatting, but evidence adjudication before pricing.
- PRICING QUARANTINE ADDED: introduced `opening-pricing-adjudication.ts` and wired it into `composeTakeoff` after all candidate recovery/promotion and before `deriveOpeningTotals`. Canonical priced `openings[]`, `total_opening_sqm`, `glazed_sqm`, visual reconciliation, external wall area, cladding deductions, and export-facing cells now use only adjudicated priced openings. Impossible non-garage witnesses are kept out of pricing for missing/invalid dimensions, impossible width, impossible height, impossible area, or impossible aspect ratio. The JM-0055-style `1.6m x 90m` witness now remains visible as review evidence with `impossible_width`, `impossible_area`, and `impossible_ratio`, but cannot affect money totals.
- FAIL-CLOSED MONEY GATE ADDED: Visual QS reconciliation errors now block opening-derived pricing instead of merely adding review text. When priced openings cannot reconcile to the QS opening count, `composeTakeoff` clears priced canonical openings, keeps every candidate in `opening_evidence` as not-priced review evidence with `visual_reconciliation_error`, and leaves `total_opening_sqm`, `glazed_sqm`, and `external_wall_area_m2` unpriced rather than computing a false total. QS export now treats enriched `openings: []` as an intentional blocked state, not permission to fall back to stale relational rows. The drop-in sheet prints `OPENING PRICING BLOCKED`, `Less openings: NOT COMPUTED`, and `NET CLADDING: NOT COMPUTED` so Fenner-style failures are loud before they reach price.
- REVIEW BRIDGE RESTORED SAFELY FOR WIZARD RUNS: `opening_schedule` was empty on JM-0055 even though `takeoff_json.openings` existed, leaving the Windows & Doors review tab and takeoff-health messaging able to disagree with the canonical export. Added `opening-schedule-projection.ts` and wired `/upload` wizard persistence to project adjudicated canonical `enriched.openings` into `opening_schedule` as source `IQ Takeoff`. Pricing/export still read `takeoff_json`; this is a UI/review projection only. Only unconfirmed prior `IQ Takeoff` projection rows are replaced; manual or confirmed rows remain untouched. Automatic text takeoff already writes its own draft opening_schedule rows and was not blindly duplicated. The projection now snapshots old unconfirmed IQ rows before replacement and restores them if the current insert fails, avoiding delete-then-empty review regressions.
- LINEAR UPDATED: `JEN-35` reopened as Urgent because strict opening promotion was not actually complete; `JEN-37` created for the pricing quarantine/adjudicator layer and updated with both implementation slices. `JEN-26`, `JEN-28`, and `JEN-29` received comments making Head Office trial readiness depend on adjudicated pricing trust rather than UI polish.
- VERIFIED AND DEPLOYED: committed `d475df8 fix(takeoff): fail closed on opening reconciliation` to `convergence` and pushed to origin. Local gates green: focused visual compose/promotion tests, projection tests, QS export fail-closed tests, touched-file ESLint, `npx tsc --noEmit`, full `npx vitest run` (`728 passed / 1 expected fail / 26 skipped`), and `npm run build` with Vite plus Cloudflare Pages postbuild geometry-proxy guard. First two normal Wrangler Pages asset uploads failed with Cloudflare API `502 Bad Gateway` from `POST /pages/assets/upload`; retrying with `--skip-caching` succeeded. Production deployment `6d71a145-c38a-4cf0-bf5f-99d6afe2fa70` on `jennian-iq-prod` served `d475df82b6eb977aa8d1b7dfa449b45b7a36d28d` at `jennian-iq-prod.pages.dev`, `jennianiq.nz`, and `www.jennianiq.nz`; deployed export asset contains `OPENING PRICING BLOCKED` and `NOT COMPUTED` cladding strings.

## 18 Jun 2026 - measurement doctrine locked into canon
- MEASUREMENT DOCTRINE LOCKED: Haydon corrected the product premise and it is now canonical, not just a Fenner/window tactic. Jennian IQ must behave like a human QS: a plan set is a set of imperfect witnesses, and one bad witness does not make the job bad. Floor-plan geometry is the backbone; text, elevations, schedules, specs, vision, and workbook truth are evidence layers. Bad floor-plan text must not delete a drawn opening. A wrong or missing elevation must not poison the floor-plan takeoff. A missing schedule must not erase measurable geometry. Unsupported dimensions stay flagged or blank, while supported local measurements continue.
- ARCHITECTURE CANON UPDATED: `ARCHITECTURE.md` now records that architecture follows the QS method, not the other way around. If current seams cannot measure what a human QS would measure, the correct fix is to extend or rebuild the measurement seam rather than accepting current code as a product limit. Existing code is useful only where it serves the QS method.
- LINEAR CANON UPDATED: `JEN-26` reframed as geometry-first opening reconciliation, with the measurement doctrine attached as a Linear document. Child slices are `JEN-30` evidence ledger (DONE in `5d6a210`), `JEN-31` floor-plan gap extraction, `JEN-32` opening router, `JEN-33` elevation cross-check, and `JEN-34` evidence output. `JEN-29` Head Office trial gate now depends on this geometry-first accuracy path.
- BUILD IMPLICATION: `JEN-31` must not be constrained to whichever vector annotations already happen to exist. If the app cannot access drawn wall gaps through the current compose/geometry seam, the seam must be extended or rebuilt from the PDF segment layer so IQ can measure the physical floor-plan gap first, then use text/elevation/schedule evidence to confirm, recover, or flag.
- OPENING EVIDENCE OUTPUT: `opening_evidence` now surfaces through the existing `reviewFlags` channel so the QS export Review Notes sheet and verification exceptions show why an opening was priced, flagged, or left incomplete. Review-only floor-plan gaps now print source path details (measured gap, elevation height support, wall face, room, status, and not-priced reason) without touching IQ paste/pricing cells. VERIFIED: focused QS export test 8 passed, `npx tsc --noEmit` green, `git diff --check` green, full `npm run test` green (`714 passed / 1 expected fail / 26 skipped`), touched-file ESLint green. Full local `npm run lint` remains blocked by known Windows CRLF repo-wide Prettier noise, not this slice. Linear `JEN-34` marked Done.
- STRICT OPENING PROMOTION: `JEN-35` adds the first controlled pricing promotion from the human-QS evidence spine. A measured floor-plan gap becomes a canonical `openings[]` row only when routing is not ambiguous, the gap is medium-confidence, one unique high/medium elevation opening supports width and height, and the elevation type is priceable (`window`, `slider`, or `external_door`; never `unknown`/`garage_door`). Promoted rows carry flags saying width came from the floor-plan gap and height/type from elevation support; ambiguous or unknown-type matches stay review-only and unpriced. VERIFIED: focused compose test 15 passed, `npx tsc --noEmit` green, `git diff --check` green, touched-file ESLint green, full `npm run test` green (`717 passed / 1 expected fail / 26 skipped`). Linear `JEN-35` marked Done.
- ELEVATION VECTOR OPENING DETECTOR V1: `JEN-36` now has the first deterministic vector detector for text-poor elevations. Added `elevation-vector-openings.ts`: it segments elevation drawings into horizontal face bands before looking for closed opening boxes, ignores roof/title/border zones, suppresses hatch/pane noise, and emits only medium-confidence `vector_face_band` candidates. Fenner proves the value and the limit: the detector finds bounded garage-door/slider/window-sized elevation candidates from the vector layer (`42` candidates, not hundreds) but is not yet wired into production upload because elevation PDF vectors are not currently passed to the elevation extractor. VERIFIED: focused detector/Fenner tests green (`7 passed / 1 expected fail`), `npx tsc --noEmit` green, `git diff --check` green, touched-file ESLint green, full `npm run test` green (`720 passed / 1 expected fail / 26 skipped`). Next slice is wiring elevation PDF vector extraction into the upload/run path and feeding candidates into the strict match/promotion gate.
- ELEVATION VECTOR UPLOAD WIRING: the elevation vector detector now runs from the supporting elevations PDF (or an elevation page inside the combined plan PDF) during `/upload`, merges its `vector_face_band` candidates into `elevationData.elevationOpenings`, and then hands that merged evidence to `composeTakeoff`. This is fail-safe: if there is no elevation PDF/page, or vector extraction fails, the old image/AI elevation path is unchanged. It still does not blindly price; candidates only influence pricing through the existing strict unique floor-plan-gap/elevation match gate. VERIFIED: focused elevation/Fenner tests green (`9 passed / 1 expected fail`), `npx tsc --noEmit` green, `git diff --check` green, touched-file ESLint green, full `npm run test` green (`722 passed / 1 expected fail / 26 skipped`).
- PRODUCTION LAUNCH / CI GATE CORRECTION: fast-forwarded `main` to the geometry-first opening stack and deployed through the production Cloudflare Pages workflow to `jennian-iq-prod`. The first `tests` run exposed two CI-only gate issues: the Supabase live type drift guard required missing `SUPABASE_ACCESS_TOKEN` in normal offline CI, and several existing files needed Prettier wrapping on Linux. Corrected `.github/workflows/test.yml` so normal code validation is offline again (`tsc`, `eslint`, `vitest`) and runs on Node 22; formatted the files CI flagged. `npm run check:supabase-types` remains an explicit ops command for schema reconciliation, not a per-push production-authority dependency. VERIFIED final `main` SHA `ac68932`: GitHub Actions `tests`, `secret-scan`, and `deploy-pages` all green; `jennian-iq-prod.pages.dev`, `jennianiq.nz`, and `www.jennianiq.nz` all serve `/version.json` build `ac68932f9828800a693eb042637940b96ba48bfa`; geometry proxy health returns 200 `{"status":"ok","version":"1.3.0"}`.

## 17 Jun 2026 - invite auth security fix, production deploy correction, Cloudflare MCP setup
- OPENINGS MONEY-GATE SLICE READY: Claude's pushback was right on the process gap and the West St regression, and wrong only where the current working tree had already de-silenced part of the schedule recovery. Reworked the slice so tests assert pricing truth, not cosmetic count truth: Beddis now gates `total_opening_sqm`, `glazed_sqm`, and `external_wall_area_m2` against the signed-off joinery/QS truth; Harrison gates the no-schedule W-code path against price area rather than flat count. Engine changes are deliberately narrow: PDF plan-text parsing now supports comma/uppercase/spaced W-code dimensions but only accepts loose forms when anchored to a nearby `W##` label (West St stays 8/8, no wet-area fixture over-match); no-schedule W-code rows become canonical vector openings; schedule rows whose head-datum heights were nulled can recover from printed W-codes, and datum-ish normalisation to 2100 is marked `height_source: asserted` with per-row flags, never silent. Provenance now carries through `OpeningSource="schedule"`, `height_source`, row flags, and persisted W-code IDs. VERIFIED red-before-green in a detached temp worktree at `83592bc` with only the new tests applied: old Harrison failed on the money gate (`15.37` vs `46.89` total opening sqm) and old Beddis failed because zero recovered schedule rows existed. VERIFIED green-after in current tree: `npx tsc --noEmit` green, full `npx vitest run` green (`689 passed / 26 skipped`), touched-file Prettier/ESLint green, live Beddis baseline green (`44.33` total openings, `108.79` external wall area), live Harrison baseline green (`46.86` total openings, `98.1` external wall area). This is not a licence to add cleverness: next accuracy work should be another evidence/provenance slice, not heuristic sprawl.
- QS EXPORT TRUTHFULNESS SLICE: Claude's correction was right that the money-path needed a cold read, not just auth/RLS. Audited `buildQSExportData` / `applyEnrichedTakeoff` / `buildDropInSheet` against the `IQ Import` contract and found three narrow QS-facing gaps: enriched garage area could not reach the paste block, missing alfresco exported as a confident `0`, and the export review page still showed first-floor absence as a generic dash. Fixed by adding `garageAreaM2` to `QSExportData`, overlaying `enriched.garage_area_m2`, writing known garage area to `IQ Import!B10`, leaving unknown garage/alfresco blank with visible verify notes, and showing alfresco/first-floor as `N/A` on `/export`. Added regression coverage for garage area passthrough and "unknown alfresco is not zero." VERIFIED: touched-file Prettier green, touched-file ESLint green, QS/export focused Vitest 97 passed, `npx tsc --noEmit` green, `npm run build` green with 689 passed / 26 skipped.
- QS EXPORT TRUTHFULNESS DEPLOYED: pushed `7e15e7c fix(qs): preserve garage and alfresco export truth` to `convergence`, rebuilt locally, and deployed to Cloudflare Pages project `jennian-iq-prod` as production deployment `e95b32d6-8740-4756-99cb-368045fcca0b` on branch `main`. LIVE VERIFIED: `jennian-iq-prod.pages.dev`, `jennianiq.nz`, and `www.jennianiq.nz` serve `jobs._jobId_.export-PETdEXwl.js` containing `Garage Area`, `First Floor Area`, `Alfresco / Deck Area`, and `N/A`; all three serve `iq-qs-export-BEB9Z7G8.js` containing the garage/alfresco verify-note strings.
- SUPABASE MIGRATION BOOKKEEPING REPAIRED / DOOR RLS CLOSED: live `supabase_migrations.schema_migrations` only recorded the first 4 migrations even though live schema had later hand-applied effects, so `db push` would have tried to replay stale historical SQL. Confirmed `SUPABASE_DB_PASSWORD` is not set locally and `npx supabase migration list --linked` still fails at direct Postgres auth (`cli_login_postgres` SASL), but Management API SQL works through the access token. Applied the missing live security migration `20260613210000_door_counts_rls_scope.sql` (no data change), verified `door_counts` now has scoped SELECT/INSERT/UPDATE/DELETE policies and no blanket `USING true`. Verified `20260616081000_admin_job_files_storage_access` and `20260617050000_enforce_active_profile_access` effects remain live. Repaired migration metadata through `supabase_migrations.schema_migrations` for all 36 local versions through `20260617050000` so old local migrations will not replay; note this is metadata-only and preserves production schema as truth, including the deliberate absence of old `project_manager` / `profiles.branch` effects.
- SUPABASE TYPE DRIFT GUARD ADDED: added `scripts/check-supabase-types.mjs` and `npm run check:supabase-types`; it loads `SUPABASE_ACCESS_TOKEN`, generates live types from project `ukegudqobnmiesudtjen`, formats with repo Prettier config, and fails if `src/integrations/supabase/types.ts` differs. Wired into `.github/workflows/test.yml` before `tsc` with `SUPABASE_ACCESS_TOKEN` secret. VERIFIED locally against live project: drift guard green, `npx tsc --noEmit` green, focused ESLint green, focused auth/access tests 6 passed. Remaining ops: set/confirm `SUPABASE_DB_PASSWORD` if we want Supabase CLI direct commands (`migration list`, `db pull`, `migration repair`) to work without the Management API path.
- SCHEMA DRIFT RECONCILIATION SLICE: Claude's F1 was confirmed. Production `profiles` has no `branch` column while the checked-in Supabase types said it did; production `app_role` is `owner/admin/estimator/viewer` while local app code still carried stale `project_manager`. `supabase migration list --linked` is currently blocked by stale direct DB auth (`SUPABASE_DB_PASSWORD` SASL failure), so no `db push` or live DDL was run. Instead, generated fresh types from the linked live project with `npx supabase gen types typescript --linked`, checked them in, removed the stale profile-branch and project-manager assumptions, and kept branch display sourced from latest `user_invitations.branch` only. VERIFIED: `npx tsc --noEmit` green, touched-file ESLint green, focused auth/access tests 6 passed, `npm run build` green with 688 passed / 26 skipped. Remaining F1 ops: reconcile Supabase migration bookkeeping/direct DB password, then add a deliberate type/schema drift guard.
- SCHEMA DRIFT SLICE DEPLOYED: pushed `38d728e fix(auth): reconcile live schema types` to `convergence`, built locally, and deployed to Cloudflare Pages project `jennian-iq-prod` in the production Haydon/Jennian account. First manual deploy with `--branch convergence` created preview `6a0c3de0` only; caught before sign-off because custom domains still served old assets. Corrected with `--branch main`, producing production deployment `07d56f44-77ba-4f32-b57e-0e71433a67d7` (source `38d728e`). LIVE VERIFIED: `jennianiq.nz` and `www.jennianiq.nz` both serve `AppLayout-CFnQJ2Ob.js`; their Users bundle `users-BTLNCcq-.js` returns 200, contains `estimator`, and no longer contains `project_manager`.
- WINDOWS LOCAL HYGIENE / AUDIT FIX: added `.gitattributes` to force LF on future touches, ignored `.codex-scratch/` plus `jennian-007-scraper/.venv/`, and widened ESLint ignores for generated/build/scratch folders so local lint no longer walks the Python venv or Codex scratch files. Ran `npm audit fix` without `--force`: lockfile updated dev/build dependencies including Babel/ws/js-yaml family, reducing audit to 3 remaining high advisories. Remaining dependency risk is deliberate: Vite/esbuild requires a breaking `vite@8` force upgrade, and `xlsx` has no upstream fix; neither was forced in this slice.
- SECURITY BUG SQUASHED: Erin's invite flow exposed a real auth boundary bug. Supabase invite verification creates a valid session before password setup; Jennian IQ treated any session as app access. Fix: `use-auth` now loads `profiles.status`, invited users are `requiresPasswordSetup`, `AppLayout` blocks private app render and redirects invited sessions to `/auth/set-password`, `login` sends invited sessions to setup instead of `/jobs`, and `auth.set-password` marks the profile `active` only after password save plus profile activation succeed. Added `src/lib/auth/activation.ts` and tests proving invited/null/suspended cannot enter while active can.
- VERIFIED LOCALLY: auth activation unit test green, targeted ESLint on auth files green with two pre-existing Fast Refresh warnings, `npx tsc --noEmit` green, `npm run build` green with 685 passed / 26 skipped. This deploy also carried the already-audited verification/page-routing dirty work from the same local slice; no commit has been made yet.
- DEPLOYMENT PITFALL FOUND: first successful manual deploy went to Cloudflare account `qiao65162@gmail.com`, Pages project `jennian-iq`, preview `fe3e400b.jennian-iq.pages.dev`. That project is not production and does not own `jennianiq.nz`. Live custom-domain probes still served old `login-K0wfg0Pt.js` with no invite guard, proving deploy-to-preview is not deploy-to-production.
- PRODUCTION DEPLOY CORRECTED: switched Wrangler OAuth to `haydon.christian@jennian.co.nz`; `pages project list` showed `jennian-iq-prod` owning `jennian-iq-prod.pages.dev`, `jennianiq.nz`, and `www.jennianiq.nz`. Deployed the built bundle to `jennian-iq-prod` -> production deployment `b698810b-458f-48c2-8980-1c72c4f3127c`, preview `https://b698810b.jennian-iq-prod.pages.dev`, source `773cb73`.
- LIVE AUTH VERIFICATION PASSED: `https://jennian-iq-prod.pages.dev`, `https://jennianiq.nz`, and `https://www.jennianiq.nz` all served `login-D3ItZxct.js` and the bundle contained both `requiresPasswordSetup` and `/auth/set-password`. Invites can resume only after existing invited users are confirmed to be in the expected profile state.
- CLOUDFLARE AGENT SETUP: fetched Cloudflare's official `agent-setup/prompt.md`, installed 11 Cloudflare skill folders under `C:\Users\Haydon\.agents\skills`, registered Codex MCP servers `cloudflare`, `cloudflare-docs`, `cloudflare-bindings`, `cloudflare-builds`, and `cloudflare-observability`, completed OAuth for all private Cloudflare MCPs, restarted Codex, and verified `codex mcp list` shows them enabled. Docs MCP is callable in-session; other Cloudflare MCP surfaces are registered/authenticated for future sessions/tool exposure.
- RUNBOOK ADDED: `docs/deployment-runbook.md` now records the production Cloudflare account/project/domain, preflight checks, manual deploy command, and live custom-domain verification script so future manual deploys do not land in the wrong account.
- OWNER ACCESS HEALTH ADDED: Haydon's post-fix owner lockout exposed an auth/profile drift blind spot: a Supabase auth user can exist with owner role but no `profiles` row, making the user correctly blocked by the password/profile gate while invisible to the Users page. Shipped owner-only `getUserAccessHealthFn` and `repairUserProfileFn`, a pure access-health classifier (`ok`, `pending_setup`, `missing_profile`, `suspended`, `orphan_profile`), Users page `Access` column + `Attention` filter, and a narrow owner-only repair action for `missing_profile`. VERIFIED: focused access/auth tests 6 passed, touched-file ESLint green, `npx tsc --noEmit` green, `npm run build` green with 688 passed / 26 skipped. DEPLOYED first as dirty production deployment `1aa26629-5439-480e-a142-d1c188d5f1a7` to `jennian-iq-prod`; live `jennianiq.nz` and `www.jennianiq.nz` both served `users-Bf5VXxtV.js`. Linear: `JEN-21` Done.
- DISABLE NOW REVOKES ACCESS: Claude's follow-up audit was correct: the old Users page Disable action only set `profiles.status='suspended'`, while RLS still authorised through `user_roles`; suspended users with roles could retain API access and the self-profile policy could allow protected field edits. Shipped app gate wiring to `canEnterApp` (only active profiles enter private routes), login no longer redirects suspended users to `/jobs`, Disable/Enable now use owner-only `setUserStatusFn` plus Supabase admin `ban_duration`, and invite activation moved to service-role `activateProfileFn`. Added migration `20260617050000_enforce_active_profile_access`: `has_role`, `is_admin_or_owner`, and `can_write` now require `profiles.status='active'`; `prevent_profile_security_self_update_trg` blocks users editing their own status/email/activation fields. VERIFIED: focused tests 6 passed, touched-file ESLint green except the existing two `use-auth` Fast Refresh warnings, `npx tsc --noEmit` green, `npm run build` green with 688 passed / 26 skipped. LIVE DB APPLIED with `npx supabase db query --linked --file supabase/migrations/20260617050000_enforce_active_profile_access.sql` because remote migration bookkeeping is stale and `db push` would replay old migrations. Live catalog verified active-profile-aware helper definitions, enabled trigger, and Haydon owner still `has_owner=true` / `is_admin_or_owner=true`. Linear: `JEN-22` Done.
- INVITE ROLE ACTIVATION GAP FOUND IN LIVE PROOF: activating `codex-validator@jennian-iq.test` through a minted production invite URL correctly set `profiles.status='active'` and landed on `/jobs`, but the account had no `user_roles` row (`can_write=false`) even though Users displayed the invited Estimator role from `user_invitations`. Patch: `activateProfileFn` now reads the latest invitation by email during password setup, updates profile name from the invite, clears stale non-owner roles, upserts the invited role, and records the role in the activation audit metadata. Live retest also caught that production `profiles` has no `branch` column despite local generated types including one, so activation deliberately leaves branch on `user_invitations` only.

## 16 Jun 2026 - JM-0050 verification follow-up, CI recovery, and state catch-up
- JM-0050 REVIEW FINDINGS: Haydon's print/workbook review exposed four product/process issues: garage dimensions/area not extracted from the plan, alfresco missing semantics, first-floor absence should not print as a blank/unknown value, and visual opening IDs/dots still confusing around the excluded garage door. Live run inspected: job `bac644bf-534d-45e1-bba4-4907ea84ec07`, takeoff run `65171750-361c-4da7-9853-db206bfc38be`, printed 16 Jun 2026 08:43 NZT. Payload had `garage_area_m2=null`, `alfresco_area_m2=null`, no first-floor field, and visual audit 20 openings / 19 QS-glazed / 1 garage door.
- SHIPPED `f2dcda0` (verification output stabilization): canonical QS opening rows now preserve visual `O` labels when the garage-door exception is excluded (JM-0050 now keeps `O7 Window Garage`, `O8 Slider Dining` instead of renumbering after `O6`); first-floor/alfresco missing values print explicit `N/A`; garage area with garage-door evidence but no measured area prints `Measure manually`; downloaded workbook compatibility rows suppress unverified internal wall values; plan-text room-dimension tolerance widened for future garage footprint reads. VERIFIED live after deploy: production `version.json` reached `f2dcda0d2d5f5ac4079a2aab971d33187fbc5164`, JM-0050 page rendered the corrected rows.
- PRODUCT DOCTRINE WARNING: the `N/A` / `Measure manually` display fixes are honesty rails, not source-of-truth extraction fixes. They must not be treated as solving garage/alfresco detection. Correct next slice is evidence-based: if the plan has an alfresco/garage footprint, extract it or flag the failed extraction path; if no evidence exists, then `N/A` is valid. Dot physical placement is also not solved by the ID fix; overlay still plots persisted `visual_opening_audit.openings[].x/y` then snaps to ink.
- CI DRIFT FOUND BY CLAUDE AUDIT: `test.yml` gates `npx tsc --noEmit` -> `npx eslint .` -> `npx vitest run`, but recent local practice used `npm run build` / Vitest as the green signal. That bypassed the real first CI gate. Current tree at `f2dcda0` failed `tsc --noEmit` with 12 errors: 5 from the JM-0050 test fixtures and 7 in vision recovery/fallback paths.
- SHIPPED `afa06fd` (type-gate recovery): fixed the out-of-scope `aiJson` runtime crash in `vision.functions.ts` by carrying a shared model diagnostic, guarded missing `ANTHROPIC_API_KEY` before fallback calls, aligned `VisionBaseGeometry.internal_wall_segments_m` with the runtime schema, guarded nullable `data?.status` in timeout recovery, and removed impossible post-branch `garage_door` checks in visual opening promotion. Test fixtures corrected to satisfy the actual types. VERIFIED: `npx tsc --noEmit` green, ESLint green on touched files, `npx vitest run` 676 passed / 26 skipped, `npm run build` green, production `version.json` reached `afa06fd311e9b692213a11925e9e9c97327aca1e`.
- ESLINT / LINE-ENDINGS NOTE: full local `eslint .` on this Windows checkout is currently dominated by CRLF Prettier errors across untouched tracked files and can run for minutes because untracked `.codex-scratch/` is also under the repo root. Touched files in `afa06fd` were normalized to LF and lint clean. CI on Linux should not see the Windows working-tree CRLF noise, but the repo still needs a deliberate `.gitattributes` / eslint-ignore cleanup rather than opportunistic mass-formatting.
- LIVE MIGRATION CHECK (16 Jun): root `STATE.md` claimed two human-paste migrations were carried. Live catalog check against Supabase project `ukegudqobnmiesudtjen` showed `20260616081000_admin_job_files_storage_access` EFFECT IS APPLIED: storage policy `Admins manage job-files objects` exists with `bucket_id='job-files' AND is_admin_or_owner(auth.uid())`. The `20260613210000_door_counts_rls_scope` EFFECT IS NOT APPLIED: live `public.door_counts` still has the blanket policy `Authenticated users can manage door counts` with `qual=true` and `with_check=true`. Attempted Management API DDL application was rejected; no live DDL change was made. ACTION STILL REQUIRED: paste/run `supabase/migrations/20260613210000_door_counts_rls_scope.sql` in Supabase SQL editor, then re-query `pg_policies`.
- SECRET ROTATION STILL UNVERIFIED: prior exposed classic GitHub PAT, GEOMETRY_API_KEY, and fine-grained PAT remain a human/dashboard confirmation item. Repo cannot prove revoke/rotation. Do not mark closed without Haydon confirming the provider dashboards or a successful credential-specific probe using the new secrets.

## 13 Jun 2026 — verification printout (P-fortnight acceptance criterion: SHIPPED)
- VERIFICATION PRINTOUT LIVE: /jobs/$jobId/verification — the human twin of the QS export. A4 print-first document (window.print → PDF): job/JMW/run-id header with NZT stamps, ⚑⚑ GEOMETRY LAYER OFFLINE banner (export-gate wording verbatim), 7 numbered sections each with a "Checked against plan ☐" box, source legend, estimator sign-off block.
- SAME-COMPOSER DOCTRINE: values come from buildQSExportData (the composer the .xlsx uses) with the SAME 5-run payload scan — paper and sheet cannot tell two stories. Provenance/confidence/⚑ per field from the enriched takeoff (GEO/VEC/VIS/SCH/DRV/AST chips). Integrity guard prints "DO NOT USE — printout/export divergence" if headline values ever disagree (floor area, window count).
- HONESTY RAILS CARRIED ONTO PAPER: ⚑ NO SOURCE door warning ("unbacked zeros, do not price"), ⚑ UNPLACED window flags, vision door hint labelled "never exported", internal walls marked "info — not exported", unanswered specs print "— not set" in red, full exceptions section from reviewFlags.
- WIRED: first action on TakeoffSummary after every run (the acceptance-criterion placement) + job-page action bar + export-page header. New pure model lib src/lib/verification/ with 14 unit tests (NZT 1pm-bug case, fail-safe doors, geometry-offline both paths, spec code→label via contract, divergence guard). Suite 577 green. Pre-existing tsc noise in myers/mcalevey .v tests untouched (not a CI gate).
- PLAN OVERLAY SHIPPED (same day): verification printout section 4 now renders the floor-plan page with every door-engine hit drawn on (solid red = confirmed, dashed amber = flag, never counted) + the plan's own W-codes boxed green (live text read, stitched for Qt one-glyph-per-item plans, zero persistence). Coordinate contract VALIDATED VISUALLY on the Alexandra bench: persisted adapter-space hits → adapterToUser → viewport.convertToViewportPoint land on Haydon's 10 Jun hand-labelled ground-truth crosses (scripts/overlay-validate.mts renders the proof; deps tsx + @napi-rs/canvas are --no-save on purpose).
- OVERLAY PERSISTENCE (additive, golden-safe): run-doors now returns pageMeta (page number, raw page.view, dims, scaleText); compose persists door_hits (ALL hits incl. flags, page-space) + door_page. Goldens run without a doorEngine → byte-identical. Pre-overlay runs degrade honestly: page renders with W-codes + a re-run note, no door markers invented. Suite 589 green (+12).
- FULL SITE AUDIT (same day): verdict production-grade. Findings: C1 door_counts blanket RLS (only job-child table with using(true) — FIXED, migration 20260613210000 mirrors module_runs; DDL AWAITING HAYDON's paste in Supabase SQL editor, CI deliberately holds no DDL credential); C2 credential-in-chat pattern (3 tokens now exposed across chats: classic ghp_…Yf3W [admin-everything, still live], GEOMETRY_API_KEY, and 13 Jun fine-grained PAT — ALL THREE need revoke/rotate, links given to Haydon); H1 CI had no tsc/eslint gate — FIXED (66 type errors zeroed via typed cell accessor in mcalevey/myers tests, repo-wide format normalization 1ada12a in .git-blame-ignore-revs, ~30 deliberate patterns grandfathered with reasoned disables, tri-gate live: tsc → eslint → vitest); H2 dual pdf.js ships ~776KB duplicate to browser (known load-bearing debt — exit path: door engine server-side, scheduled spike); M1 SheetJS CVEs are LOW practical risk (uploads PDF-only, xlsx write-only — escalates to critical if .xlsx uploads ever land); M2 geometry-api.ts zero tests (contract test wanted: 401 → geometry_status unavailable, locks in the demo-week lesson). STRONG: RLS 21/21, 195 auth.uid() refs, server-enforced destructive ops, fail-closed roles, 0 empty catches, 0 console.log, no secrets in history.
- AUDIT FOLLOW-THROUGH (same evening): M2 CLOSED — geometry client contract tests (9) pin every failure path: 401/403 → null + a warning that NAMES the GEOMETRY_API_KEY fix (the demo-week 401 ran silent for two days because no status was ever logged — now it's in the console in seconds), 500/network/success:false/malformed-JSON → null with reasons, pinned-page failure → exactly one observable auto-detect retry. Client behaviour unchanged (null contract intact, seam flag still fires). C2 STRUCTURAL GUARD — secret-scan.yml gates every push/PR with gitleaks v8.21.2 over FULL history (verified clean locally before gating: 59 commits, no leaks). H2 CORRECTED — audit overstated it: neither pdf.js bundle is in the initial load (entry chunk has no static pdf import; v5 loads per-route on demand, v4 only when a takeoff runs). True cost ≈776KB on-demand transfer per takeoff session where ≈406KB would do. Server-side door engine remains the exit, urgency downgraded MEDIUM.
- ROUND THREE (same night): LIVE same-composer lock — tests/live/verification-model.live.test.ts asserts on the REAL JM-0020 every live-validate run: model builds clean, integrityAlerts EMPTY (export and takeoff agree on production data), floor area real, overlay marker tallies exact, specs render labels never codes. SUPPLY CHAIN — all 13 action references across 10 workflows SHA-pinned (SHAs fetched live from GitHub, not memory); dependabot.yml watches npm + actions weekly behind the four gates. ARCHITECTURE.md written from audit-depth knowledge: topology, the full takeoff pipeline, security model, CI/CD, debts-and-decisions table, conventions — the map to STATE's log.
- 29A WEST ST DEEP AUDIT (job JM-0032, Pearse McGougan): full job folder obtained (SharePoint cookie-jar + plan uploads). PLAN TRUTH established. Engine fixed: isAnnotationContext kills 3 confirmed-confidence false doubles (Pacific bath caption 1655; dim-chain 1318/1500 flanked by 90s) — doubles 5→2 (the real Bed3 robe 1420 + linen 1620). West Street = bench plan #2 with knownGap contract (singles truth 11, engine 9 — Bed3 + WIR misses locked, zero flags is the bug). Wardrobe routing CLOSED by Haydon's own QS: robes are shelf+rail, robe door sets price on the Double Doors line → doubles→B29 stands. TAKEOFF errors vs truth: Bed3 window missing (misrouted to Bed1 qty2 + phantom Garage 1.5×1.3), Ensuite height 1.8 vs 1.1, garage_area 46.7 = title-block CLADDING AREA grabbed by vision (real garage 4.0×5.95=23.8). HAYDON's estimate fixed + delivered (29a...IQ AUDITED.xlsm, recalc clean vs baseline, 48 errors pre-existing his template): garage door 4.8-insulated qty→0 (plan = 3.0 door in a 4.0 garage; SPEC DOC promises an impossible 4.8 — must be corrected to 3.0×2.1 insulated pre-contract), carried at 2.7-insulated $2,422 as flagged FLOOR (no 3.0 cost line exists); Standard 7→11, Doubles 0→2, Cavities 0→2. Net live-calc delta +$2,258.36; quoted H-column snapshot locked at $403,907 by template design → that's the quantified silent erosion if unfixed. Client-estimate B15 hardcoded (doesn't track inputs).
- PLAN-TEXT MODULE (same night, the flawless mandate): the floor plan prints its own truth — src/lib/takeoff/plan-text.ts parses room footprints ("GARAGE 4 000 X 5 950" → 23.8 m²), printed joinery codes ("1 300x1 500", H×W), and the title-block AREA table from the door adapter's existing label extraction (zero extra cost; parsed in run-doors, persisted additively as plan_text). Three compose cross-checks kill the three JM-0032 vision faults: (1) garage title-block grab → corrected to printed room dims, source vector, loud reconciliation flag; (2) any routed window whose dims match no printed code → flagged (catches Ensuite 1.8 vs printed 1 100x600); (3) any printed BED room with no routed window → flagged with canonical bedroom matching, MASTER≡BED1 (catches the missing Bed 3). Parser proven on BOTH fixture plans (West St ground truth exact; Alexandra 15 clean rooms unseen). Golden safety: no planText → byte-identical behaviour, locked by test. ALSO: radius-slack arc fix for the 2 missed singles attempted + REVERTED — a 760 stole an 810's arc (radii 1.5pt apart at 1:100) creating a phantom cavity; lesson written at the gate, misses stay as the bench knownGap. Suite 611.
- WINDOW AUTO-FIX (same night, "flags aren't fixes — FIX IT"): routeWindowCodes spatially assigns every printed joinery code to its nearest room label (cupboard rooms excluded) — 8/8 exact on the real West St PDF against Haydon's hand-verified workbook (open-plan KITCHEN/DINING/LOUNGE boundary fuzz accepted, same glazing, real slots). correctWindowsByRoom then corrects vision INTO windows_by_room BEFORE aggregation on schedule-less jobs (a real schedule still outranks): missing rooms ADDED (Bed 3 auto-fixed), dims corrected (Ensuite 1.8→1.1), phantom quantities collapsed (Master qty 2→1) — every change logged verbatim as a ⚑ FIXED flag, source flips to vector. Mismatch + bedroom alarms re-based onto the corrected map so a fixed window is never re-flagged. Counts, openings, glazing, QS slots all flow from corrected routing. Suite 613.
- P2 RIBBON-TRACE v1 SHIPPED (same night): traceInteriorWalls measures interior wall lm deterministically from the vector layer — stud-spaced pair detection (70–115mm), exclusive nearest-partner pairing (combinatorial pairing read 833 lm), room-anchored connected-component selection (raw length picked the COMPASS ROSE), room-label sided interior test (geometric beyond-test lost to the L-shape at 360 lm), stack collapse for cupboard shelving phantoms. West St: 68.7 lm / 32 ribbons, visually audited overlay — every real partition traced, envelope/dims/compass clean, residual ~+25% joinery+shower bias DOCUMENTED. Alexandra: 89.1 / 31, plausible for ~200m². Both locked EXACT as benches. Wired: run-doors returns wallTrace; compose internal_wall_lm = ribbon value (source vector, conf mid, verbatim bias flag) when present; printout shows "ribbon-trace v1 (verify; not exported)" with the value; QS EXPORT SUPPRESSION REMAINS until live validation against hand measurements. From 7 lm garbage to 68.7 verify-grade in one night. Suite 615.
- PRELIM-SET HANG (post-midnight, Haydon's full preliminary plan set): two live runs stuck at 'running' with NULL payloads — ribbon-trace v1's O(n²) pairing detonated on working-drawing segment density (concept sheets ~4k axials; full sets tens of thousands), locking the browser before persist. FIXED: sweep-line candidate generation (sorted-offset window, identical results — both ribbon benches byte-stable at 68.7/89.1), plus hard density caps that SKIP honestly instead of hanging (axials 30k / candidates 120k / pieces 4k → trace returns empty, internal walls fall back to the suppressed pre-v1 state, the RUN COMPLETES). plan-text gets a 25k label cap with the same degrade-to-vision semantics. Perf contract locked: 40k segments → instant skip <800ms; 15k segments → 157ms via sweep. Doctrine written into the module: a trace that cannot finish fast must not run at all. The two corpse runs in the DB are harmless (the 5-run payload scan skips NULL payloads) — Haydon just re-runs after deploy. Suite 619.
- OPEN (carried): ribbon-trace v2 (kill joinery/shower bias, then un-suppress export with HC sign-off), vision misroute fixes (Bed3/title-block garage area → cross-check flags or label-seeded detector rooms), PAT regenerate w/ Workflows + engine-repo scope → authenticated deploy gate + geometry-probe matrix; GEOMETRY_API_KEY rotation (exposed in two chats) + Railway twins cleanup; Beddis/Young golden re-runs + re-freeze; door-engine wiring as live source; bed3/wardrobe-1620/laundry answers from HC; P-fortnight remainder (next: plan-overlay page — door/window hits drawn on the floor plan, door_flags already carry page-space coords).
