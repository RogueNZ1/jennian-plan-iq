# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Supabase CLI cache
supabase/.temp/

# Claude Code local settings (machine-specific permissions)
**/.claude/settings.local.json

# Local env — secrets must never be committed
.env
.env.local
.env.*.local

# Playwright
/tests/e2e/fixtures/*.pdf
/test-results/
/playwright-report/

# Phase 1 harness — rendered plan image is derived from the (gitignored) plan PDF;
# the golden/replay JSON fixtures ARE committed so the offline replay test runs in CI.
/tests/phase1/__fixtures__/*.b64.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
